#include "version.h"

// NOTE: version.cpp.in is used to autogenerate the version.cpp file found in the build/release dir

namespace RPI::Version {

// define Info struct's static git members

const std::string GIT_SHA1 = "@GIT_SHA1@";
const std::string GIT_DATE = "@GIT_DATE@";
const std::string GIT_COMMIT_SUBJECT = "@GIT_COMMIT_SUBJECT@";

}; // end of RPI::Version namespace
